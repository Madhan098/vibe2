services:
  - type: web
    name: codemind-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 8000
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.11.0

